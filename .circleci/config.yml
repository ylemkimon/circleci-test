version: 2
jobs:
  test:
    parameters:
      browser:
        type: string
    docker:
      - image: circleci/node:6
    steps:
      - checkout
      - date
      - echo << parameters.browser >>

workflows:
  version: 2
  test:
    jobs:
      - test
